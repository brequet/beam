# Normalize line endings: LF everywhere, except cmd batch files.
* text=auto eol=lf
*.cmd text eol=crlf

# Binary assets (none yet; listed so future additions are handled correctly).
*.png binary
*.ico binary
